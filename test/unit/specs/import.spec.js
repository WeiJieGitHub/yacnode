import Index from 'views/index';
import style from 'views/index.scss';

describe('unit test import', () => {
  it('unit test can import scripts', () => {
    expect(true).toBe(true);
  });
  it('unit test can import styles', () => {
    expect(true).toBe(true);
  });
});
